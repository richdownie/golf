<table id="teams-table">
  <% @teams.each do |team| %>
  <tr>
    <td><%= team.name %></td>
    <td><%= team.state %></td>
    <td><%= team.team_avg %></td>
  </tr>
  <% end %>
</table>
<br />

<%= button_to 'Add Your Team', new_team_path %>